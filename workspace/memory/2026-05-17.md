# 2026-05-17 — Daily log

## Boot

This file is seeded by hand at the end of the session that built me. Future
entries are written by Clawbot itself.

## What happened

- Set up OpenClaw as git submodule under `openclaw/`, built dist.
- Installed Ollama; pulled `gemma4:e2b` (7.2 GB), `phi4-mini` (2.5 GB),
  `qwen3:4b` (2.5 GB) for benchmarking. Dropped `gemma4:e4b` and
  `gemma4:latest` for disk pressure.
- Direct-API bench (`scripts/bench-model.sh`): gemma4:e2b warm 620 ms,
  phi4-mini warm 641 ms, qwen3:4b warm 1520 ms (and rambles in `thinking`
  field even with `think:false` — disqualified).
- Built switcher (`scripts/claw-model.sh`): 5 slots (cpu / gemma / phi /
  gpu-local / cloud). Cloud is dormant per user.
- Picked `qwen2.5:7b-instruct-q4_K_M` as the GPU-local target — no
  thinking mode, fits 8 GB VRAM at Q4_K_M, strong tool calling.
- Hit the OpenClaw 120s agent-harness timeout repeatedly. Diagnosed:
  not a real timeout but `fetchWithSsrFGuard` default + giant system
  prompt. Applied lean config: `experimental.localModelLean`, raised
  provider `timeoutSeconds: 360`, capped `num_ctx: 8192`, set
  `keep_alive: "30m"`, disabled 6 plugins.
- Even with lean config, agent turn on 4-CPU container still >200 s.
  Decided: CPU is config/code-only, agent live-test moves to PC/GPU.
- User reframed: workspace files = mind, not setup. Moved `MODELS.md`
  and `setup-gpu.md` out to `/docs/`.
- Built cognitive workspace (this commit): SOUL.md, USER.md, MEMORY.md,
  TOOLS.md, HEARTBEAT.md, AGENTS.md, this daily log, knowledge/-tree.
- Enabled `agents.defaults.memorySearch` via `nomic-embed-text` over
  Ollama for semantic recall.

## Decisions co-signed by user

- voll lokal, kein Cloud
- GPU-Produktiv-Ziel = RTX 3070 / 8 GB VRAM / 16 GB RAM, kein CPU-Offload
- Sweet-Spot-Architektur (Tier B: standard workspace + RAG +
  heartbeat-cron + summarization) — 12-layer Edge ist Upgrade-Option,
  nicht jetzt

## Open follow-ups

- Verify on PC: full agent-turn timing through the new cognitive
  workspace with qwen2.5:7b on GPU.
- Wire up Telegram bot token + run a first paired session.
- After 1-2 weeks of usage: review daily logs, promote stable facts to
  MEMORY.md, condense oldest logs.
- Decide whether 12-Layer-Edge upgrade (LightRAG + facts.db + lossless-claw)
  is worth the operational complexity.

## Memory promotion candidates

(None yet — heartbeat will populate this section starting tomorrow.)

## Daily summary

(Written by Clawbot's end-of-day heartbeat. Empty on the seed day.)
